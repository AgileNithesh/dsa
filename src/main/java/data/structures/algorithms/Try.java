package data.structures.algorithms;

import java.util.Arrays;

public class Try {

    static void main() {
        String[] s = new String[]{"a", "v", "n"};
        IO.println(Arrays.toString(s));
        s[1] = null;
        IO.println(Arrays.toString(s));
        IO.println("".length());

        IO.println(Integer.MAX_VALUE);
    }


}
